[project]
name = "prep-pilot"
version = "0.1.0"
description = "Daily senior-SWE interview trainer with spaced repetition and an LLM tutor."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "sqlmodel>=0.0.22",
    "httpx>=0.28",
    "bcrypt>=4.2",
    "itsdangerous>=2.2",
    "apscheduler>=3.11",
    "python-multipart>=0.0.18",
    "pydantic-settings>=2.7",
    "pymupdf>=1.28.0",
]

[tool.uv]
package = false
